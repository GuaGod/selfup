<view class="container">
   <view class="title">
      选择一种颜色(｡･∀･)ﾉﾞ
   </view>
   <view class="colorBox">
      <block wx:for="{{boxObjArr}}">
         <s-box boxData="{{item}}"
                bindtapColorBox="onTapColorBox"></s-box>
      </block>
   </view>
</view>