<view class="container">
     <view class="content"
           style="background:{{_missionData.thingColor}}"
           bindtap="onTapMission"
           data-thingid="{{_missionData.thingId}}"
           >{{_missionData.thingName}}</view>
</view>

 